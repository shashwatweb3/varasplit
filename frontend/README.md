# VaraSplit Frontend

## Required env vars

```bash
NEXT_PUBLIC_VARA_RPC=wss://testnet.vara.network
NEXT_PUBLIC_PROGRAM_ID=0x...
NEXT_PUBLIC_VOUCHER_ID=0x... # optional
```

The app is intended to run against a deployed on-chain VaraSplit program and does not include demo mode.
